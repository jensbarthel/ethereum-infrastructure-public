apiVersion: v1
kind: Secret
metadata:
  name: {{ include "validator.fullname" . }}
  labels:
  {{- include "validator.labels" . | nindent 4 }}
type: Opaque
data:
  {{ .Values.validator.wallet.walletSecretSubPath}}: "{{ .Values.validator.wallet.walletPassword | b64enc }}"